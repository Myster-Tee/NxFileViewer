<UserControl x:Class="Emignatik.NxFileViewer.Views.OpenedFileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:Emignatik.NxFileViewer.Views"
             xmlns:loc="clr-namespace:Emignatik.NxFileViewer.Localization"
             mc:Ignorable="d"
             Style="{StaticResource UserControlStyle}"
             d:DesignHeight="500" d:DesignWidth="400" d:DataContext="{d:DesignInstance views:OpenedFileViewModel}">
    <TabControl>
        <TabItem Header="{Binding Source={x:Static loc:LocalizationManager.Instance}, Path=Current.Keys.TabOverview}" >
            <views:FileOverviewView DataContext="{Binding Path=FileOverview}"/>
        </TabItem>
        <TabItem Header="{Binding Source={x:Static loc:LocalizationManager.Instance}, Path=Current.Keys.TabContent}">
            <views:ContentView DataContext="{Binding Path=Content}"/>
        </TabItem>
    </TabControl>
</UserControl>
