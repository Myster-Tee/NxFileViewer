<Border x:Class="Emignatik.NxFileViewer.Views.CustomControls.GroupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="200">

    
    <d:DesignerProperties.DesignStyle>
        <Style TargetType="Border">
            <Setter Property="Background" Value="White" />
        </Style>
    </d:DesignerProperties.DesignStyle>
    
    <DockPanel>
        <TextBlock x:Name="HeaderTextBlock" Text="Any title" Style="{StaticResource Title1}" DockPanel.Dock="Top" />
        <ContentControl x:Name="ContentControlContent" />
    </DockPanel>

</Border>
